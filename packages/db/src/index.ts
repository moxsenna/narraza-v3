// @narraza/db — Prisma client, repositories implementing application ports, and
// the Auth.js adapter (D8: web imports DB only through this public API). Schema
// and migration runner land in W0.3; repos in M2.

export const DB_PACKAGE = '@narraza/db' as const;
