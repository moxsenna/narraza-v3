// @narraza/ai — provider adapters (OpenRouter/Gemini), routing, prompt
// projectors, output parsing, and model-policy allowlist. No ledger/DB/artifact
// access. Populated in M4; adapters stay behind AI_ENABLE_MOCK until staging.

export const AI_PACKAGE = '@narraza/ai' as const;
