// @narraza/shared — DTOs, zod schemas, env schemas (per process), i18n message
// codes, and cross-cutting utils. Populated across M0–M6; this barrel re-exports
// the stable public surface.

export const SHARED_PACKAGE = '@narraza/shared' as const;
