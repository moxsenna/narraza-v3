// M0 scaffold placeholder. The real landing (hero + cara kerja + CTA, copy from
// the prototype) is built in W0.5, and finalized in M6.
export default function HomePage() {
  return (
    <main className="mx-auto max-w-3xl px-6 py-24">
      <p className="text-sm font-semibold tracking-wide text-brand-700 uppercase">Narraza v3</p>
      <h1 className="mt-2 font-serif text-4xl font-bold text-brand-900">
        Build long fiction without losing the plot.
      </h1>
      <p className="mt-4 text-neutral-600">Scaffold M0 — struktur siap, konten menyusul (W0.5).</p>
    </main>
  );
}
