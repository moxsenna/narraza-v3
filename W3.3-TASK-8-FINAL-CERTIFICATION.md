# W3.3 Task 8 — Rollback Durability Candidate Certification

**Candidate Commit:** populated after final commit
**Branch:** `feat/m3-w3.3`
**Date:** 2026-08-24
**Status:** Candidate — awaiting external PM approval

## Candidate scope

Task 8 final correction scope contains:

- `packages/db/src/repos/ledger-reconciliation.integration.test.ts`
- `packages/db/src/repos/ledger-port.ts`
- `W3.3-TASK-8-FINAL-CERTIFICATION.md`

No frontend, worker, schema, migration, or Task 9+ changes belong to this candidate.

## Runtime contract

Authoritative release reasons remain exactly:

- `invocation_completed`
- `final-close`
- `queued-cancel`

`cancellation_refund` is not part of application or database port contracts.

## Cases 04 and 07

Both rollback durability cases:

1. Receive harness `client` and `databaseUrl` from one `createSchemaTestSuite()` registration.
2. Seed conflicting ledger row through harness `client.query()` outside Unit of Work.
3. Assert harness count equals one immediately after insert.
4. Assert Prisma sees same row before entering Unit of Work.
5. Mutate reservation and attempt divergent replay inside one Unit of Work.
6. Convert `binding_invalid` into private rollback sentinel and throw.
7. Assert reservation state is restored after rollback.
8. Assert exact dedupe count remains one.
9. Assert seeded ledger tuple remains unchanged.

No `ENVIRONMENT BLOCKED` waiver, commented mandatory assertion, or debug `console.log` remains.

## Exact candidate evidence

Evidence must come from final committed candidate tree.

| Gate                                    |                 Required result |
| --------------------------------------- | ------------------------------: |
| Task 8 PostgreSQL suite                 |  27 passed, 0 failed, 0 skipped |
| Full DB aggregate                       | 244 passed, 0 failed, 0 skipped |
| `pnpm typecheck`                        |                            PASS |
| `pnpm lint`                             |                            PASS |
| Prettier check for changed Task 8 files |                            PASS |
| `git diff --check`                      |                            PASS |

## External review state

This document records candidate evidence only. It does not declare PM approval, Task 8 closure, PR authorization, or merge authorization. External PM review remains required after candidate commit is published and remote parity is verified.
